<nav class="navbar navbar-expand-lg navbar-light bg-light shadow">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('visitor.welcome.index') }}"><img src="{{ asset('favicon/favicon-32x32.png') }}" alt="Logo France"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link"  href="{{ path('visitor.welcome.index') }}">Retour au site</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {# <li class="nav-item">
                    <a class="nav-link"  href="{{ path('visitor.welcome.index') }}">Espace privé</a>
                </li> #}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app.logout') }}">Déconnexion</a>
                </li>
            </ul>
            
        </div>
    </div>
</nav>